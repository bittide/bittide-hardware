#!/usr/bin/env bash
# SPDX-FileCopyrightText: 2024 Google LLC
#
# SPDX-License-Identifier: Apache-2.0
ROOT=$(git rev-parse --show-toplevel)
cd "${ROOT}" || exit 1

echo "Formatting Rust files.."
"${ROOT}"/cargo.sh fmt --all -- --emit files

pre-commit run format-hs --all-files
